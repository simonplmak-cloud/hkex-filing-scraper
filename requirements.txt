# Vercel build dependencies (used by `buildCommand`: `mkdocs build --strict`).
# The Python *functions* are bundled from the project's dependency graph
# (pyproject.toml + uv.lock, including the default `deploy` dependency-group), not from
# this file.
mkdocs-material==9.7.7
