# Vercel build dependencies.
# Vercel bundles the Python *functions* from the project's own dependency graph
# (pyproject.toml + uv.lock), not from this file — so this file only needs the docs
# toolchain used by `buildCommand` (`mkdocs build --strict`).
mkdocs-material==9.7.7
